# Phase 3 authoring — Python 3.10+ dependencies for scripts/ (issue #20, EPIC C #3).
# Pins align with the ajananth/water-quality-assistant data-seeder reference.
# These libraries are exercised in Phase 4 (live load + ontology deploy), not Phase 3.
pandas>=2.0
pyarrow>=14.0
deltalake>=0.17
azure-identity>=1.15
requests>=2.31
python-dotenv>=1.0

# Phase 5 — Foundry agent harness + eval framework (issue #24, EPIC D #4).
azure-ai-projects>=2.2.0   # hosted-agent client / PromptAgentDefinition (verified §2a)
openai>=1.40               # Responses/Chat client returned by AIProjectClient.get_openai_client()
duckdb>=1.0                # deterministic SQL ground truth over the synthetic CSVs

# Phase 8 — README-as-article chart generation (issue #33, EPIC G #7).
matplotlib>=3.7            # scripts/make_charts.py renders the committed result charts (docs/assets/)
