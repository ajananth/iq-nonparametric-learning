# Secrets & environment — never commit (Constitution Art. VI)
# NOTE: synthetic data under data/ IS committable (Art. V) and is intentionally NOT ignored.
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Phase-6 optimizer candidate scratch (materialized configs); committed outputs live in eval/optimization_runs/
.optimize_workspace/
